/**
 * Converts Persian year to Gregorian year. 
 * @param persianYear 
 * @returns 
 */
const calculateGregorianYear = (persianYear: number) => persianYear + 621;

export default calculateGregorianYear;
